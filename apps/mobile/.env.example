# API base URL for the mobile app.
# Leave blank to auto-detect: the app derives the host from the Expo dev
# server and talks to port 4000 on the same machine (works across networks).
# Set this only to point at a backend on a different machine, e.g.
EXPO_PUBLIC_API_URL=
